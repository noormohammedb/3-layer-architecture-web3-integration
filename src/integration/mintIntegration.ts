const mintIntegration = (id: number, amount: number) => {
  console.log("mintIntegration ");
  console.log("id, amount : ", id, amount);

  const retrunData = { data: "blockchain result" };

  return retrunData;
};

export default mintIntegration;
