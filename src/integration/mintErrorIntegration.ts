const mintErrorIntegration = (id: number, amount: number) => {
  console.log("mintErrorIntegration ");
  console.log("id, amount : ", id, amount);

  const retrunData = { data: "blockchain result" };

  return retrunData;
};

export default mintErrorIntegration;
